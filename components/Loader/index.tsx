import { LoaderComponent, LoaderContainer } from "./LoaderComponents";

const Loader = () => {
    return (
        <>
            <LoaderContainer>
                <LoaderComponent></LoaderComponent>
            </LoaderContainer>
        </>
    );
};

export default Loader;
